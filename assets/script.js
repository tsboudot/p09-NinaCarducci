const filterButtons = document.querySelectorAll(".filter-btn"); function filterGallery(e) { const t = e.target.id, l = document.querySelectorAll(".gallery > div"); filterButtons.forEach(e => { e.id === t ? e.classList.add("active") : e.classList.remove("active") }), "Tous" === t ? l.forEach(e => { e.style.display = "block" }) : l.forEach(e => { const l = e.getAttribute("data-gallery-tag"); e.style.display = l === t ? "block" : "none" }) } filterButtons.forEach(e => { e.addEventListener("click", filterGallery) }); const galleryItems = document.querySelectorAll(".gallery .gallery-item"); function openModalWithImage(e) { const t = e.currentTarget.getAttribute("data-image-url"); document.getElementById("modalImage").src = t, new bootstrap.Modal(document.getElementById("imageModal")).show() } galleryItems.forEach(e => { e.addEventListener("click", openModalWithImage) });